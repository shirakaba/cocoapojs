/**
 * @satisfies {import("prettier").Config}
 * @see https://prettier.io/docs/en/configuration.html
 */
const config = {
  // Supply empty config to prevent Prettier climbing up to user configs.
};

export default config;
